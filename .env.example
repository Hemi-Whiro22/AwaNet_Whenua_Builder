# Environment Configuration Example
# Copy this file to .env and fill in your actual values

# Environment (development, staging, production)
ENVIRONMENT=development

# API Keys - REQUIRED
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_ORG_ID=your_openai_org_id_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Google Cloud Vision Credentials
# Path to your Google Cloud service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=the-den-faa84-39e2d1939316.json

# CORS Security - Comma-separated list of allowed origins
# Leave empty or unset to disable CORS (recommended for production with API gateway)
# Example: ALLOWED_ORIGINS=https://yourfrontend.com,https://www.yourfrontend.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Trusted Hosts - Comma-separated list of allowed hostnames
# Set to specific hosts in production for security
# Example: TRUSTED_HOSTS=api.yourdomain.com,www.yourdomain.com
TRUSTED_HOSTS=*

# Output Directory for generated files (audio, scribe logs)
OUTPUT_DIR=backend
