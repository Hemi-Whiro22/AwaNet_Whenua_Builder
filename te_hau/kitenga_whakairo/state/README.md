# Whakairo Codex State

Place cached schema snapshots or MCP state here (e.g., Supabase schema dumps, vector index fingerprints). Avoid storing secrets. If you export schema, include timestamp and source in the filename.
